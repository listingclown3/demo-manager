# demo-manager
This lets me serve demos on [my website](chrisccluk.live) by scanning subdirectories and hosting it's HTML/JS files.

# how to setup
mkdir demo-manager

cd demo-manager

git clone any repository of your choice that kind of follows the index.html format because that is where it will static serve the demo

node server.js
